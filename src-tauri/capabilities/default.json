{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The page the companion serves is the app itself, so it may call the app's own commands. Nothing else is opened to it: the shell and the dialog are used from Rust only.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://localhost:*",
      "http://127.0.0.1:*"
    ]
  },
  "permissions": [
    "core:default",
    "allow-open-folder"
  ]
}
