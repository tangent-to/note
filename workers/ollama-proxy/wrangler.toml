name = "ollama-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# Optional: restrict CORS to your site's origin instead of allowing any origin.
# [vars]
# ALLOWED_ORIGIN = "https://notebook.tangent.to"
